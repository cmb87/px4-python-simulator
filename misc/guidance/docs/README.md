# Guidance Notes

This folder contains short theory notes for guidance examples in `src/guidance/`.

- Main note: `zem_pnav.tex`

## Build

From this directory:

```bash
pdflatex zem_pnav.tex
```

The output PDF is `zem_pnav.pdf`.
